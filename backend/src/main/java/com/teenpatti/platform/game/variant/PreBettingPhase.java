package com.teenpatti.platform.game.variant;

/**
 * Pre-betting phases for variants that delay the first turn (discard, auction, etc.).
 */
public enum PreBettingPhase {
    NONE,
    DISCARD,
    AUCTION
}
